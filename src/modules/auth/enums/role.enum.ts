/**
 * User Role Enum
 *
 * OOP Concepts:
 * - Encapsulation: Role values in one place
 * - Type Safety: Prevents invalid role values
 */
export enum Role {
  CUSTOMER = 'CUSTOMER',
  ADMIN = 'ADMIN',
  OWNER = 'OWNER',
}
